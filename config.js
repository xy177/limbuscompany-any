// GitHub Pages is HTTPS. This API URL must also be HTTPS, otherwise browsers will block it.
// Recommended target after reverse proxy / Cloudflare Tunnel:
// window.RANDOM_IDENTITY_API_BASE = "https://your-domain.example";
window.RANDOM_IDENTITY_API_BASE = "";
